# paseo-sandbox-fixture

Synthetic repository used to verify the Paseo + Vercel Sandbox integration.

The `add` function in `src/math.mjs` is intentionally broken. The test in
`test/math.test.mjs` fails until the agent fixes the implementation.

Expected agent task: make `npm test` pass.
